# [API Reference](@id api_reference)

本页面包含 BSAHelper.jl 的完整 API 参考文档。

绘图扩展通过扩展模块实现，本页面不收录其 docstring。

## 索引

```@index
Modules = [BSAHelper, BSAHelper.BSACore, BSAHelper.BSABootstrap]
Order = [:module, :type, :function]
```

## 完整 API 文档

```@autodocs
Modules = [BSAHelper, BSAHelper.BSACore, BSAHelper.BSABootstrap]
Order = [:module, :constant, :type, :function]
```
